use day_four::{self};
use day_one::{self};
use day_three::{self};
use day_two::{self};

fn main() {
    // Day One
    day_one::part_one();
    day_one::part_two();

    // Day Two
    day_two::part_one();
    day_two::part_two();

    // Day Three
    day_three::part_one();
    day_three::part_two();

    // Day Four
    day_four::part_one();
    day_four::part_two();
}
